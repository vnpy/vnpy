# 多因子策略投研

vnpy.alpha模块面向量化研究、机器学习建模和因子特征工程场景，供用户在本地Python环境中进行数据处理、特征计算、模型训练和策略研究。

AlphaDataset支持通过`add_feature`和`set_label`添加字符串形式的因子表达式。该能力用于本地受信用户自行编写和执行量化因子表达式，并非面向远程调用的服务接口。

在二次开发中，不应将Web请求、开放API、远程配置或其他不可信来源的字符串内容未经校验直接作为因子表达式执行。如需支持外部表达式输入，应由业务系统先完成白名单限制、表达式校验、人工审核或隔离执行等安全措施。
